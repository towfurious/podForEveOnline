<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Compose handles all Material3 theming itself.
        This XML theme only controls the Android window chrome (status bar, nav bar)
        before Compose draws its first frame.
        NoActionBar → no title bar flicker. windowBackground black → no white flash.
    -->
    <style name="Theme.PodForEve" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/ember_bg</item>
        <item name="android:statusBarColor">@color/ember_bg</item>
        <item name="android:navigationBarColor">@color/ember_surface</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
