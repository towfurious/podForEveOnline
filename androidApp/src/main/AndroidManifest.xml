<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- SCHEDULE_EXACT_ALARM (revocable) — USE_EXACT_ALARM is restricted by Play policy to
         core alarm/clock/calendar apps and isn't appropriate here. Guarded at call time via
         AlarmManager.canScheduleExactAlarms(), falls back to inexact delivery if not granted. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".PodForEveApplication"
        android:label="PodForEve"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.PodForEve"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- OAuth2 PKCE callback: eve-tracker://callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="eveauth-podforeve" android:host="callback" />
            </intent-filter>
        </activity>

        <!-- Skill training countdown ForegroundService.
             See wiki: [[ADR-006 - Android Foreground Service]], [[ADR-015 - Unified Completion Notifications]] -->
        <service
            android:name="com.podforeve.tracker.platform.service.SkillTrainingService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Ongoing countdown notification for the user's active EVE Online skill-queue training." />
        </service>

        <receiver
            android:name="com.podforeve.tracker.platform.NotificationAlarmReceiver"
            android:exported="false" />

    </application>

</manifest>
