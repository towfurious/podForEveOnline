root = true

[*]
charset                  = utf-8
end_of_line              = lf
indent_style             = space
insert_final_newline     = true
trim_trailing_whitespace = true

[*.{kt,kts}]
indent_size      = 4
max_line_length  = 140
ktlint_code_style = intellij_idea

# Composable functions use PascalCase — suppress the naming rule for them
# (Detekt handles this separately via ignoreAnnotated)
ktlint_standard_function-naming = disabled
